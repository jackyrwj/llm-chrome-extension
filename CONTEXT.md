# HF Model Assistant

面向国内开发者的浏览器扩展：在 Hugging Face / ModelScope 模型详情页注入侧边栏，帮助用户判断"这个模型我的硬件能不能跑、怎么跑起来"。

## Language

**核心场景**:
国内开发者在模型详情页评估并部署模型。所有功能取舍以此为第一标准；国外市场不做针对性投入（已有 NEBUL-AI 等竞品，且镜像映射、翻译对国外用户无价值）。
_Avoid_: 通用模型助手、模型发现平台

**部署画像**:
核心用户是国内小团队/实验室，用自有或租用的 GPU 服务器搭建推理服务。Deploy 功能以 vLLM/SGLang 等服务化工具为核心；Ollama 等本地体验工具降级为辅助。VRAM 估算需面向推理服务（KV cache、并发、多卡），而非单机单卡跑通。
_Avoid_: 个人玩家、玩具场景

**核心功能**:
仅由可准确获取的硬数据构成的四块功能：模型硬信息（config 有什么显示什么，没有则不显示）、权重文件实际总大小、镜像映射下载命令（无映射时用 HF 命令兜底）、基础部署命令。一切推断性能力（正则猜参数量、VRAM/KV cache/并发估算、硬件推荐）均已明确排除。
_Avoid_: VRAM 估算、智能推荐、硬件建议

**硬数据**:
可以从 HF API（config.json、文件列表及大小）、页面官方标注或人工维护的映射表直接获得的精确数据。判断功能去留的第一标准：展示的值必须有确定来源，不允许猜测。
_Avoid_: 推断值、估算值

**嵌入卡片**:
插件的唯一 UI 形态：注入到 HF/ModelScope 模型页右侧信息栏顶部的一张卡片，样式协调中性、深浅色自适应，不使用原站 class（避免原站改版时样式破碎）。SPA 重渲染抹掉卡片时由 MutationObserver 重新注入。已废弃侧边栏形态及其页面推挤逻辑。
_Avoid_: 侧边栏、悬浮球、popup

**卡片区块**:
卡片内自上而下固定为三个区块：模型硬信息（含社交统计一行）、下载区（权重实际大小 + 镜像/HF 命令）、部署命令区（vLLM / SGLang 切换，只生成基础启动命令，不推荐参数）。顺序对应用户决策流：是什么 → 怎么拉 → 怎么跑。

**镜像映射**:
HF 模型到 ModelScope 上对应模型的对照关系（`src/data/mapping.json` + 搜索兜底），用于生成国内可达的下载命令。是国内场景的核心差异化能力。
_Avoid_: 下载加速、代理
